name: Main

on:
  push:
    branches: [main]

permissions:
  id-token: write  # Required for npm OIDC trusted publishing (caller workflow)
  contents: write
  pull-requests: write

jobs:
  release:
    name: Release
    uses: ./.github/workflows/release.yml
    secrets: inherit
